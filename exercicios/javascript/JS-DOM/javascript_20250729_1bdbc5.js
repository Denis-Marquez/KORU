// Obtém o elemento com o ID "meuElemento" e altera seu conteúdo
const elemento = document.getElementById("meuElemento");
if (elemento) {
  elemento.textContent = "Texto alterado via getElementById!";
}